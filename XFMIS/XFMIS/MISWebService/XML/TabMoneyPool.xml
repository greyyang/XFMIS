<?xml version="1.0" encoding="utf-8" ?>
<!--============================================================================
//	CAUTION: This file is generated by IBatisNetGen.BatisMap.cst at 2009-4-14 11:30:53
//				Any manual editing will be lost in re-generation.
//===========================================================================-->
<sqlMap namespace="TabMoneyPool"
	xmlns="http://ibatis.apache.org/mapping"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

  <alias>
    <typeAlias alias="TabMoneyPool" type="MISEntity.Entity.TabMoneyPool, MISEntity" />
  </alias>

  <resultMaps>
    <resultMap id="FullResultMap" class="TabMoneyPool">
      <result property="MPID" column="MP_ID" dbType="int"/>
			<result property="MPMoney" column="MP_Money" dbType="decimal"/>
			<result property="MPHandler" column="MP_Handler" dbType="varchar"/>
			<result property="MPDate" column="MP_Date" dbType="datetime"/>
			<result property="MPMemo" column="MP_Memo" dbType="varchar"/>
			<result property="MPFlag" column="MP_Flag" dbType="int"/>

    </resultMap>
  </resultMaps>

  <statements>
    <insert id="Insert" parameterClass="TabMoneyPool">
      INSERT INTO Tab_MoneyPool([MP_Money],[MP_Handler],[MP_Date],[MP_Memo],[MP_Flag],[Column_7]) VALUES (#MPMoney#,#MPHandler#,#MPDate#,#MPMemo#,#MPFlag#)
      <selectKey resultClass="int" type="post" property="MPID" >
        select @@IDENTITY as value
      </selectKey>
    </insert>
    <update id="Update" parameterClass="TabMoneyPool">
      UPDATE Tab_MoneyPool SET MP_Money = #MPMoney#, MP_Handler = #MPHandler#, MP_Date = #MPDate#, MP_Memo = #MPMemo#, MP_Flag = #MPFlag# WHERE 1=1			 AND MP_ID = #MPID# 
    </update>
    <delete id="Delete" parameterClass="int">
      DELETE [dbo].[Tab_MoneyPool] WHERE MP_ID = #MPID#
    </delete>

    <select id="FindById" parameterClass="int" resultMap="FullResultMap">
      SELECT * FROM [dbo].[Tab_MoneyPool] WHERE MP_ID = #MPID#
    </select>

    <select id="Find" parameterClass="System.Collections.Hashtable" resultMap="FullResultMap">
      SELECT * FROM [dbo].[Tab_MoneyPool] WHERE 1=1
      <dynamic>
        				<isNotNull prepend="and" property="MPID">
					MP_ID = #MPID#
				</isNotNull>
				<isNotNull prepend="and" property="MPMoney">
					MP_Money = #MPMoney#
				</isNotNull>
				<isNotNull prepend="and" property="MPHandler">
					MP_Handler = #MPHandler#
				</isNotNull>
				<isNotNull prepend="and" property="MPDate">
					MP_Date = #MPDate#
				</isNotNull>
				<isNotNull prepend="and" property="MPMemo">
					MP_Memo = #MPMemo#
				</isNotNull>
				<isNotNull prepend="and" property="MPFlag">
					MP_Flag = #MPFlag#
				</isNotNull>

      </dynamic>
    </select>

    <select id="FindCount" parameterClass="System.Collections.Hashtable" resultClass="int">
      SELECT COUNT(*) FROM [dbo].[Tab_MoneyPool] WHERE 1=1
      <dynamic>
        				<isNotNull prepend="and" property="MPID">
					MP_ID = #MPID#
				</isNotNull>
				<isNotNull prepend="and" property="MPMoney">
					MP_Money = #MPMoney#
				</isNotNull>
				<isNotNull prepend="and" property="MPHandler">
					MP_Handler = #MPHandler#
				</isNotNull>
				<isNotNull prepend="and" property="MPDate">
					MP_Date = #MPDate#
				</isNotNull>
				<isNotNull prepend="and" property="MPMemo">
					MP_Memo = #MPMemo#
				</isNotNull>
				<isNotNull prepend="and" property="MPFlag">
					MP_Flag = #MPFlag#
				</isNotNull>

      </dynamic>
    </select>
  </statements>
</sqlMap>