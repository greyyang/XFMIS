<?xml version="1.0" encoding="utf-8" ?>
<!--============================================================================
//	CAUTION: This file is generated by IBatisNetGen.BatisMap.cst at 2009-4-14 11:30:53
//				Any manual editing will be lost in re-generation.
//===========================================================================-->
<sqlMap namespace="TabBusinessMode"
	xmlns="http://ibatis.apache.org/mapping"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

  <alias>
    <typeAlias alias="TabBusinessMode" type="MISEntity.Entity.TabBusinessMode, MISEntity" />
  </alias>

  <resultMaps>
    <resultMap id="FullResultMap" class="TabBusinessMode">
      <result property="BPID" column="BP_ID" dbType="int"/>
			<result property="BPValue" column="BP_Value" dbType="varchar"/>

    </resultMap>
  </resultMaps>

  <statements>
    <insert id="Insert" parameterClass="TabBusinessMode">
      INSERT INTO Tab_BusinessMode([BP_Value]) VALUES (#BPValue#)
      <selectKey resultClass="int" type="post" property="BPID" >
        select @@IDENTITY as value
      </selectKey>
    </insert>
    <update id="Update" parameterClass="TabBusinessMode">
      UPDATE Tab_BusinessMode SET BP_Value = #BPValue# WHERE 1=1			 AND BP_ID = #BPID# 

    </update>
    <delete id="Delete" parameterClass="int">
      DELETE [dbo].[Tab_BusinessMode] WHERE BP_ID = #BPID#
    </delete>

    <select id="FindById" parameterClass="int" resultMap="FullResultMap">
      SELECT * FROM [dbo].[Tab_BusinessMode] WHERE BP_ID = #BPID#
    </select>

    <select id="Find" parameterClass="System.Collections.Hashtable" resultMap="FullResultMap">
      SELECT * FROM [dbo].[Tab_BusinessMode] WHERE 1=1
      <dynamic>
        				<isNotNull prepend="and" property="BPID">
					BP_ID = #BPID#
				</isNotNull>
				<isNotNull prepend="and" property="BPValue">
					BP_Value = #BPValue#
				</isNotNull>

      </dynamic>
    </select>

    <select id="FindCount" parameterClass="System.Collections.Hashtable" resultClass="int">
      SELECT COUNT(*) FROM [dbo].[Tab_BusinessMode] WHERE 1=1
      <dynamic>
        				<isNotNull prepend="and" property="BPID">
					BP_ID = #BPID#
				</isNotNull>
				<isNotNull prepend="and" property="BPValue">
					BP_Value = #BPValue#
				</isNotNull>

      </dynamic>
    </select>
  </statements>
</sqlMap>